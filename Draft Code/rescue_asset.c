#include<stdio.h>
#include<stdlib.h>
#include"resscue_asset.h"


rescue_asset *fget_assets(FILE *file){
	if(file == NULL){
		return NULL;
	}
	rescue_asset *root;
	file.getchar(c)
	while(c!=EOF){
		while(c!=" "){
			char fred[12] += c;
		}
	}
}
